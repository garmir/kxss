name: Unique Test - kxss
on: [push, workflow_dispatch]

jobs:
  unique-test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: DeterminateSystems/nix-installer-action@main
        continue-on-error: true

      - name: Unique Test Execution - kxss
        continue-on-error: true
        run: |
          nix-shell -p curl jq coreutils --run '
            echo "=== Unique Test: kxss ===" > unique-test-kxss.txt
            echo "Repository: ${{ github.repository }}" >> unique-test-kxss.txt
            echo "Test ID: TEST_153" >> unique-test-kxss.txt
            echo "Timestamp: $(date)" >> unique-test-kxss.txt
            curl -s ifconfig.me >> unique-test-kxss.txt
            echo "Unique instruction: General framework compliance and performance testing" >> unique-test-kxss.txt
          '

      - name: Ubuntu Fallback - kxss
        if: failure()
        run: |
          sudo apt-get update -qq
          sudo apt-get install -y curl jq
          echo "Ubuntu fallback for kxss" > fallback-kxss.txt
          curl -s ifconfig.me >> fallback-kxss.txt

      - name: Upload Results
        uses: actions/upload-artifact@v4
        with:
          name: unique-test-kxss
          path: "*.txt"
          retention-days: 2